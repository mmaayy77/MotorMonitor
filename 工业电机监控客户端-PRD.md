# 基于 Qt 的工业电机远程监控与规则化故障诊断客户端 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.1 |
| 产品版本 | MVP V1.0 |
| 文档日期 | 2026-08-11 |
| 开发周期 | 10 个工作日 |
| 主演示平台 | Windows 10/11 |
| 验证平台 | Ubuntu 22.04 或 24.04 |
| 产品形态 | Qt 单机桌面客户端 + 独立控制台设备模拟器 |

### 修订记录

| 版本 | 日期 | 修订内容 |
|---|---|---|
| V1.0 | 2026-08-11 | 完成产品范围、功能、协议、线程、存储、测试及验收设计 |
| V1.1 | 2026-08-11 | 集中锁定 TCP 连接模型、规则诊断边界、告警术语和实时数据处理口径 |

## 1. 产品概述

### 1.1 产品定位

本产品是一套面向小型工业现场的跨平台桌面监控软件。单台电脑通过 TCP 同时管理 10～100 台工业电机设备，完成设备连接管理、实时遥测、规则化故障诊断、告警管理、历史查询和远程控制。

首版采用单机客户端架构，不包含独立采集服务。配套模拟器以单个进程模拟多台电机，每台虚拟电机对应一条独立 TCP 连接，仅用于功能、异常及性能测试。

首版中的“故障诊断”特指：基于遥测指标、设备状态和时间窗口规则，对过热、过流、堵转、振动异常、电压异常和通信异常进行识别，并展示诊断依据；不包含机器学习、预测性维护或剩余寿命估算。

### 1.2 产品目标

1. 在一台普通开发电脑上稳定管理 100 条独立设备连接。
2. 完成从设备接入、实时监控、异常识别、告警处理到历史追溯的闭环。
3. 用明确的线程边界和批处理策略保证高频数据场景下 UI 可响应。
4. 建立可自动验证的协议、重连、告警、存储和稳定性测试体系。
5. 形成能够支撑 Qt、网络、并发、协议、数据库和工程化面试讨论的作品。

### 1.3 非目标

MVP 不实现：

- 独立采集服务、微服务和消息队列。
- 用户登录、角色权限和控制审批。
- 云端同步和多客户端数据同步。
- FFmpeg、OpenCV、AI 和预测性维护。
- MCU、串口、Modbus 和其他工业协议。
- QML、复杂插件系统和插件市场。
- 自动升级、固件升级和设备参数管理。
- 告警通知推送、批量设备控制和规则图形化编辑器。
- 长期数据归档、自动清理和复杂容量治理。

### 1.4 未来演进边界

UI 和业务层不直接操作 Socket，而是依赖 `IDeviceGateway`。未来升级为“客户端 + 独立采集服务”时，可以替换网关实现，而不重写页面、告警展示和历史查询业务。

### 1.5 MVP 不可变原则

以下原则是开发与验收的统一口径，不因具体页面或类设计而改变：

1. **一台设备一条 TCP 连接。** 模拟器以单进程承载 10～100 台虚拟电机，但每台电机与客户端之间均使用独立连接；不得使用一条连接复用 100 台设备。
2. **首版只做规则诊断。** 所有诊断结果必须能够展示指标、阈值、连续次数、时间窗口和恢复依据；不得使用“AI 预测”或“预测性维护”描述首版能力。
3. **告警确认不等于告警恢复。** 用户确认只表示已经查看；指标是否恢复仅由规则引擎根据恢复条件判断。
4. **UI 合并不等于数据丢弃。** UI 只显示每台设备的最新快照，但通信层仍解析全部合法报文，历史数据、告警事件、控制结果和设备事件按既定策略持久化；任何异常丢失都必须显式记录。

## 2. 用户与核心场景

### 2.1 目标用户

- 小型工厂设备运维人员。
- 集中查看电机状态的值班人员。
- 排查设备通信和运行异常的技术人员。

首版默认使用者拥有全部操作能力，不设计登录和权限体系。

### 2.2 用户问题

- 无法集中掌握多台电机的在线和运行状态。
- 异常发生后不能快速定位设备、指标和诊断依据。
- 实时数据缺少历史留存，故障难以回溯。
- 网络波动后需要人工恢复连接。
- 控制指令缺少执行结果、失败原因和超时反馈。
- 大量设备持续上报时，界面和数据库容易互相阻塞。

### 2.3 主演示流程

1. 启动模拟器并创建 100 台虚拟电机。
2. 客户端建立 100 条 TCP 连接，完成握手和设备注册。
3. 设备总览显示在线状态、运行状态和最新遥测。
4. 选择一台电机，查看实时指标和最近 60 秒曲线。
5. 下发启动和目标转速指令，展示 RequestId、结果和耗时。
6. 注入过载，观察电流升高、温度上升、规则诊断和告警触发。
7. 确认告警，再恢复工况，展示恢复与关闭流程。
8. 注入断线，展示离线识别、指数退避和自动重连。
9. 查询该设备历史遥测、告警和控制记录并导出 CSV。
10. 展示连接数、消息速率、数据库写入速率、错误数和稳定性报告。

## 3. 设备模型

### 3.1 设备标识与配置

每台设备配置包含：

- 唯一设备 ID，格式为 `MOTOR-0001`。
- 显示名称。
- IP 地址和 TCP 端口。
- 是否启用、是否自动连接。
- 额定电流和告警阈值。

多个设备可以使用相同 IP 和端口，但每台设备建立独立 TCP 连接。

### 3.2 遥测字段

| 字段 | 类型 | 单位/范围 | 说明 |
|---|---|---|---|
| deviceId | UTF-8 字符串 | 最长 32 字节 | 设备唯一标识 |
| operatingState | 枚举 | Stopped、Starting、Running、Stopping、EmergencyStopped | 电机运行状态 |
| healthState | 枚举 | Normal、Warning、Fault | 诊断健康状态 |
| temperature | float32 | 20～120 ℃ | 电机温度 |
| voltage | float32 | 180～260 V | 输入电压 |
| current | float32 | 0～20 A | 工作电流 |
| speed | uint16 | 0～3000 RPM | 实际转速 |
| targetSpeed | uint16 | 0～3000 RPM | 目标转速 |
| vibration | float32 | 0～20 mm/s | 振动速度有效值 |
| runtimeSeconds | uint64 | 秒 | 累计运行时间 |
| deviceAlarmBits | uint32 | 位掩码 | 设备原始告警位，仅作为诊断输入 |
| timestamp | int64 | Unix 毫秒 | 设备数据时间 |

连接状态、运行状态和健康状态相互独立。UI 的综合状态按“离线 > 故障 > 告警 > 启动中 > 运行 > 停止”的优先级显示。

### 3.3 模拟数据关联

- 启动后转速逐渐接近目标值。
- 目标转速升高时，电流、温度和振动缓慢增加。
- 停止后转速逐渐归零，温度缓慢下降。
- 过载时电流先升高，随后温度升高。
- 堵转时实际转速显著下降，同时电流迅速升高。
- 轴承异常时振动升高。
- 电压异常时产生欠压或过压数据。
- 数据加入小幅、可配置的随机噪声；固定随机种子时结果可复现。

## 4. 连接、握手与状态

### 4.1 TCP 连接模型

模拟器以一个进程监听一个端口。客户端为每台已启用设备创建一个独立连接，因此 100 台设备对应 100 个 `QTcpSocket`。全部 Socket 由同一个通信线程中的 Qt 事件循环管理，不为单个设备创建线程。

### 4.2 身份握手

1. 客户端根据设备配置建立 TCP 连接。
2. 客户端发送 `ConnectRequest`，携带目标设备 ID、协议版本和 RequestId。
3. 模拟器为目标 ID 绑定虚拟电机并返回 `DeviceRegister`。
4. 客户端校验设备 ID、协议版本和重复连接。
5. 校验通过后进入 `Online`，否则关闭连接并记录原因。

以下情况不得进入在线状态：设备 ID 不匹配、协议版本不兼容、同一 ID 已存在有效连接、注册超时、报文非法。

### 4.3 连接状态

```text
Disabled
Disconnected
Connecting
Registering
Online
Reconnecting
Disconnecting
ProtocolError
```

手动断开后不自动重连；异常断开和协议错误按可重试错误处理。离线设备保留最后有效遥测，但必须显示数据已过期及最后更新时间。

### 4.4 心跳与重连

- 在线设备每 2 秒进行一次心跳交互。
- 距离最后有效消息达到 5 秒时判定离线。
- 重连间隔依次为 1、2、4、8、16、30 秒，之后保持 30 秒上限。
- 任意成功注册会重置退避计数。
- 网络恢复后，在下一次重连周期内恢复连接。
- 重连时继续请求原设备 ID，保证身份映射稳定。

## 5. 信息架构

### 5.1 一级页面

1. **设备总览**：展示全部设备的最新状态和关键指标。
2. **实时监控**：展示选中设备的详情、曲线、诊断依据和控制操作。
3. **告警中心**：处理活动告警并查询历史告警。
4. **历史查询**：查询遥测、状态、告警和控制记录，支持 CSV 导出。
5. **运行日志**：查看连接、协议、存储、控制和性能信息。

设备配置、告警阈值和客户端参数通过对话框管理，不增加一级页面。

### 5.2 全局状态栏

所有一级页面均显示：在线设备数、总设备数、活动告警数、当前接收速率、存储状态和当前时间。

## 6. 功能需求

| 编号 | 模块 | 功能 | 验收条件 |
|---|---|---|---|
| FR-01 | 设备管理 | 添加、编辑、删除、启用、禁用和批量生成设备配置 | ID 不重复；地址、端口和阈值完成校验；重启后配置保留；删除在线设备前断开并二次确认 |
| FR-02 | 连接管理 | 手动连接/断开、自动连接、注册、心跳、超时和重连 | 100 条独立连接；5 秒内识别离线；退避序列正确；手动断开不重连 |
| FR-03 | 协议处理 | 长度帧、拆包、校验、消息解码和错误隔离 | 半包、粘包、多帧、非法长度、错误 CRC、未知类型和截断报文均有测试，且不崩溃、不无限增长缓冲区 |
| FR-04 | 设备总览 | 显示 ID、连接、运行、健康、五项指标、活动告警数和更新时间 | 支持搜索和状态筛选；约 5 Hz 批量刷新；离线数据明确标记过期 |
| FR-05 | 实时监控 | 最新指标、最近 60 秒曲线、诊断依据和最近控制结果 | 切换设备时可响应；每设备保留最多 300 个曲线采样；内存不随运行时间无限增长 |
| FR-06 | 远程控制 | 启动、停止、设置目标转速、急停、清除可恢复告警和查询状态 | 唯一 RequestId；3 秒超时；展示等待、成功、失败和超时；按钮按状态启禁 |
| FR-07 | 规则诊断 | 过热、过流、堵转、振动、电压、心跳和遥测停更诊断 | 展示规则、实际值、阈值、连续次数、触发时间和恢复依据 |
| FR-08 | 告警管理 | 活动与历史告警；确认、恢复、关闭和筛选 | 确认不代表恢复；恢复由规则引擎产生；只有已确认且已恢复的告警可以关闭 |
| FR-09 | 历史存储 | 异步批量存储遥测、告警事件、状态变化和控制结果 | 磁盘操作仅在存储线程；事务批量提交；退出前冲刷队列；不得静默丢弃 |
| FR-10 | 历史查询 | 按设备、时间、数据类型和告警状态查询；CSV 导出 | 查询期间 UI 可操作；结果分页；CSV 为 UTF-8 且与筛选条件一致 |
| FR-11 | 运行日志 | 分级记录连接、协议、重连、控制、告警、存储和性能 | 支持级别及设备 ID 筛选；滚动保存；默认不逐帧记录遥测正文 |
| FR-12 | 性能观测 | 连接数、消息速率、写入速率、错误数、重连次数、队列水位和 UI 刷新耗时 | 每秒更新统计；统计功能不逐事件刷新 UI 或写入日志 |

## 7. 实时数据策略

### 7.1 频率与处理口径

| 环节 | 目标频率 | 处理要求 |
|---|---:|---|
| TCP 接收与协议解析 | 每设备 1～5 帧/秒，总计 100～500 帧/秒 | 接收并解析全部合法帧，不受 UI 刷新频率影响 |
| 最新状态缓存 | 每条合法遥测更新一次 | 每台设备只保留最新快照及脏标记，供总览刷新 |
| UI 批量刷新 | 约 5 Hz，即每 200 毫秒一次 | 合并两次刷新之间的中间状态，不逐帧重绘 |
| 实时曲线采样 | 每设备 1～5 点/秒 | 保存最近 60 秒，每设备最多 300 个采样点 |
| SQLite 持久化 | 累计 200 条或等待 500 毫秒时提交 | 使用事务批量写入；频率控制只改变提交批次，不丢弃合法记录 |

### 7.2 数据流规则

- 通信层接收并解析全部合法遥测帧，目标总速率为 100～500 帧/秒。
- `TelemetryCache` 保存每台设备的最新状态和脏标记。
- UI 每 200 毫秒交换一次脏状态快照并批量刷新，单帧遥测不直接触发重绘。
- 每台设备维护最近 60 秒、最多 300 个采样点的内存环形缓冲区，用于快速切换曲线。
- 每条合法遥测进入存储队列；UI 合并中间状态不等于丢弃历史数据。
- 控制响应、告警事件和设备状态变化使用可靠事件流，不允许覆盖。
- 跨线程只传递不可变值对象或任务，不跨线程直接使用 UI、Socket 和数据库对象。

## 8. 远程控制

| 指令 | 允许状态 | 参数 | 结果 |
|---|---|---|---|
| Start | 在线、Stopped、健康状态非 Fault | 无 | 成功、状态不允许、设备故障、超时 |
| Stop | 在线、Starting/Running，包括 healthState=Warning | 无 | 成功、状态不允许、设备故障、超时 |
| SetTargetSpeed | 在线、Starting/Running | 0～3000 RPM | 成功、参数越界、状态不允许、设备故障、超时 |
| EmergencyStop | 在线、Starting/Running，包括 healthState=Warning | 无 | 成功、状态不允许、超时 |
| ClearRecoverableAlarm | 在线、healthState 为 Warning/Fault 且异常条件已恢复 | 无 | 成功、条件未恢复、设备故障、超时 |
| QueryStatus | 任意在线状态 | 无 | 成功、超时 |

- RequestId 为 64 位无符号整数，在客户端进程生命周期内不重复。
- 默认响应超时为 3 秒。
- 客户端不自动重发可能改变设备状态的控制指令。
- 模拟器缓存每台设备最近 1024 个 RequestId 及响应；收到重复 RequestId 时返回原响应，不重复执行。
- UI 显示 RequestId、发送时间、完成时间、耗时、结果码和失败原因。
- 急停操作必须二次确认，并使用区别于普通按钮的明显危险样式。

## 9. 规则化故障诊断

默认采样判断以每条合法遥测为一次。连续次数在条件不满足时清零。

| 规则 | 级别 | 触发条件 | 恢复条件 |
|---|---|---|---|
| 高温预警 | Warning | 温度连续 3 次 > 85 ℃ | 温度连续 3 次 < 80 ℃ |
| 严重过热 | Critical | 温度任意 1 次 > 100 ℃ | 温度连续 3 次 < 90 ℃；恢复后允许执行设备清除指令 |
| 过流 | Warning | 电流连续 3 次 > 设备额定电流 | 电流连续 3 次 < 额定电流的 90% |
| 堵转 | Critical | 目标转速 ≥ 500 RPM，实际转速连续 3 次 < 目标值的 10%，且电流 > 额定电流的 120% | 实际转速恢复至目标值的 80%以上或设备停止，连续 3 次 |
| 振动异常 | Warning | 振动连续 5 次 > 7.1 mm/s | 振动连续 5 次 < 6.0 mm/s |
| 电压异常 | Warning | 电压连续 3 次 < 200 V 或 > 240 V | 电压连续 3 次位于 205～235 V |
| 心跳超时 | Critical | 5 秒未收到任何有效消息 | 重新连接、注册并收到有效心跳 |
| 遥测停更 | Warning | 在线状态下 5 秒未收到遥测，但心跳正常 | 收到下一条合法遥测 |

每个告警显示规则名称、设备 ID、触发指标、实际值、阈值、连续次数、首次触发时间、最近更新时间、峰值和诊断说明。

## 10. 告警生命周期

### 10.1 术语定义

- **触发（Triggered）**：规则满足触发条件，创建或激活告警。
- **确认（Acknowledged）**：用户已查看告警；不代表异常消失，也不修改诊断结果。
- **恢复（Recovered）**：规则满足恢复条件，设备指标回到安全范围；由规则引擎自动判定。
- **关闭（Closed）**：告警已经恢复且已经确认，转入不可操作的历史记录。

### 10.2 状态迁移

```text
Active-Unacknowledged
  ├─ 用户确认 → Active-Acknowledged
  └─ 指标恢复 → Recovered-Unacknowledged

Active-Acknowledged
  └─ 指标恢复 → Recovered-Acknowledged

Recovered-Unacknowledged
  └─ 用户确认 → Recovered-Acknowledged

Recovered-Acknowledged
  └─ 系统归档或用户关闭 → Closed
```

- 规则引擎负责触发和恢复，用户负责确认。
- 确认表示“用户已查看”，不改变设备异常状态。
- 同一设备、同一规则在活动期间不得重复创建告警；后续样本更新最近值、峰值和持续时间。
- 关闭仅允许发生在告警已恢复且已确认之后。
- 每次状态迁移单独保存为告警事件，便于审计和历史回放。

## 11. 自定义二进制协议

### 11.1 帧格式

协议使用网络字节序，单帧最大 65,536 字节。

| 字段 | 长度 | 说明 |
|---|---:|---|
| Magic | 2 字节 | 固定魔数 `0x4D54` |
| Version | 1 字节 | 首版为 1 |
| TotalLength | 4 字节 | 包含头部、Payload 和 CRC32 |
| MessageType | 2 字节 | 消息类型 |
| Sequence | 4 字节 | 每连接递增序列号，用于统计重复和间隙 |
| RequestId | 8 字节 | 请求响应匹配；非请求消息为 0 |
| Timestamp | 8 字节 | Unix 毫秒时间戳 |
| Payload | 变长 | 按消息类型定义的定长或带长度字段二进制结构 |
| CRC32 | 4 字节 | 对 CRC 字段之前的完整帧计算 |

主要消息类型：`ConnectRequest`、`DeviceRegister`、`HeartbeatRequest`、`HeartbeatResponse`、`TelemetryReport`、`ControlRequest`、`ControlResponse`、`StatusQuery`、`StatusResponse`、`ProtocolError`。

### 11.2 解析策略

- 每条连接维护独立接收缓冲区。
- 缓冲区不足完整头部时等待下次读取。
- `TotalLength` 小于最小帧或大于 65,536 时记录错误并关闭连接。
- 缓冲区不足完整帧时保留数据并等待。
- 完整帧通过 CRC 后解码；一次读取中的后续帧继续循环解析。
- 魔数错误时在受限范围内搜索下一个魔数并记录丢弃字节数。
- CRC 错误和未知消息被丢弃并计数。
- 10 秒内连续 3 个协议错误时关闭连接并进入重连。
- 单连接接收缓冲区上限为 128 KiB，超过上限立即断开，防止无限增长。

## 12. 线程与组件架构

### 12.1 UI 线程

负责页面、Qt Model/View、曲线和 200 毫秒刷新定时器。不得执行 Socket 收发、SQLite 操作或逐帧规则计算。

### 12.2 通信线程

运行 Qt 事件循环，拥有 `DeviceManager`、全部 `QTcpSocket`、`ConnectionWorker`、`ProtocolCodec`、`RequestTracker`、`TelemetryCache` 和 `AlarmEngine`。

单条遥测的处理顺序为：收包 → 拆帧 → CRC 与语义校验 → 更新最新缓存 → 执行诊断规则 → 投递持久化记录。

### 12.3 存储线程

拥有唯一 SQLite 连接、写入队列和查询执行器。数据库连接只在存储线程创建、使用和关闭。

写入满足任一条件时提交事务：累计 200 条记录或距离上次提交达到 500 毫秒。队列容量为 50,000 条，高水位为 80%。正常性能测试期间不得达到容量上限。

### 12.4 错误和降级

- 网络断开：更新状态、产生通信告警并重连。
- 控制超时：标记失败，不自动重发状态改变指令。
- 数据库繁忙：存储线程内最多重试 3 次，每次间隔 50、100、200 毫秒。
- 磁盘不可写或队列满：显示持久化故障横幅，记录开始时间、受影响类型和未持久化条数；禁止静默丢弃。
- 程序退出：停止新连接和控制，冲刷写入队列，关闭数据库，再停止通信线程和 UI。
- 异常退出后重启：已提交事务保持有效，未提交事务由 SQLite 回滚。

## 13. SQLite 数据设计

### 13.1 核心表

- `device_config`：设备配置、连接参数和阈值。
- `telemetry`：设备 ID、时间戳、运行/健康状态及全部指标。
- `alarms`：告警当前状态、触发值、阈值、峰值及各阶段时间。
- `alarm_events`：告警生命周期的每次状态迁移。
- `command_records`：RequestId、指令、参数、结果、错误码和耗时。
- `device_events`：上线、离线、重连、状态变化和协议错误。

### 13.2 索引

- `telemetry(device_id, timestamp)`。
- `alarms(device_id, triggered_at)`。
- `alarms(state, triggered_at)`。
- `alarm_events(alarm_id, event_time)`。
- `command_records(device_id, sent_at)`。
- `device_events(device_id, event_time)`。

历史查询默认限制时间范围为最近 24 小时，每页 200 条；用户可以扩大时间范围，但查询始终异步执行。

## 14. 日志系统

- 日志级别：Debug、Info、Warning、Error、Critical。
- 日志分类：App、Connection、Protocol、Command、Alarm、Storage、Performance。
- 支持按级别、分类和设备 ID 筛选。
- 文件日志按 10 MiB 滚动，最多保留 5 个历史文件。
- 默认不逐帧记录遥测正文，只记录速率汇总和异常帧摘要。
- 协议错误日志不得输出未受限的大块原始数据，单次十六进制摘要最多 128 字节。

## 15. 模拟器需求

模拟器是独立控制台程序，不提供图形界面，不承担历史存储和客户端业务告警判断。

支持：

- 单进程创建 10～100 台虚拟电机。
- 一个端口接受多条连接，并按客户端目标设备 ID 绑定电机。
- 遥测、心跳、设备注册和控制响应。
- 正常、过载、过热、堵转、振动异常和电压异常。
- 网络延迟、主动断线、停止心跳和停止遥测。
- 错误 CRC、非法长度和截断报文。
- 配置设备数量、上报频率、监听地址、随机种子、故障设备和故障开始时间。
- 输出当前连接数、发送速率、控制次数和注入故障统计。

模拟器可以使用现有 Reactor 网络框架管理连接；该实现选择不改变客户端 PRD 和协议行为。

## 16. 非功能需求

### 16.1 性能与稳定性

- 支持 100 台虚拟设备，每台一条 TCP 连接。
- 每台设备每秒上报 1～5 帧，总速率达到 100～500 帧/秒。
- UI 约 5 Hz 批量刷新，窗口拖动、页面切换、设备切换和查询操作保持可响应。
- SQLite 使用事务批量写入，不在 UI 或通信线程执行磁盘操作。
- 连续运行 4 小时无崩溃、死锁、Socket 失控增长或持续性内存增长。
- 正常磁盘环境和目标负载下，合法遥测、告警、控制结果和设备事件的未记录数量为 0。

### 16.2 跨平台

- Windows 10/11 完成全部功能和主演示验证。
- Ubuntu 22.04 或 24.04 完成 CMake 构建、自动化测试，以及连接、遥测、诊断、告警和存储核心流程验证。
- 两个平台使用同一源码、CMake 配置、协议和 SQLite 表结构。

### 16.3 技术基线

- C++17。
- Qt 6 Widgets、Qt Network、Qt SQL。
- QCustomPlot 绘制实时曲线。
- SQLite。
- CMake。
- GoogleTest。

## 17. 测试计划

### 17.1 单元测试

- 长度帧正常解析、半包、粘包和多帧。
- 非法长度、错误 CRC、未知消息和缓冲区上限。
- RequestId 匹配、重复响应和超时。
- 连接状态机和指数退避序列。
- 电机运行状态机和固定随机种子。
- 全部诊断规则及恢复阈值。
- 告警触发、确认、恢复和关闭。
- SQLite 批量写入、事务回滚和查询。

### 17.2 集成测试

- 客户端和模拟器完成注册、心跳、遥测和控制闭环。
- 人为拆分及合并 TCP 数据，验证拆包逻辑。
- 主动断线后验证离线识别和自动重连。
- 注入业务故障，验证诊断依据和告警生命周期。
- 写入遥测时并发执行历史查询。
- 100 台设备建立 100 条独立连接。

### 17.3 四小时稳定性测试

测试场景为 100 台设备、每台每秒 5 帧、总速率约 500 帧/秒，期间随机断线、恢复和注入故障。

记录：

- CPU 型号和核心数。
- 平均及峰值 CPU 占用。
- 初始及 4 小时后内存占用。
- 接收、解析和数据库写入速率。
- UI 刷新耗时。
- 当前及峰值存储队列长度。
- 重连次数和成功率。
- 协议错误数量。
- 未记录遥测、告警、控制和设备事件数量。

## 18. MVP 验收标准

1. 100 台设备对应 100 条独立 TCP 连接并完成身份注册。
2. 总接收速率稳定达到 100～500 帧/秒。
3. UI 约 5 Hz 刷新，高负载期间主要交互保持可响应。
4. 所有合法控制响应通过 RequestId 匹配，默认 3 秒超时。
5. 心跳超时后 5 秒内识别离线。
6. 按 1、2、4、8、16、30 秒完成指数退避重连。
7. 网络恢复后在下一次重连周期内恢复连接。
8. 支持 8 条规则，覆盖过热、过流、堵转、振动、电压及通信/遥测异常，并展示诊断依据。
9. 支持告警触发、确认、恢复、关闭和历史查询。
10. SQLite 在独立线程中批量写入，正常负载下无记录丢失。
11. 历史数据、告警和控制记录可筛选并导出 UTF-8 CSV。
12. 4 小时运行无崩溃、死锁、Socket 失控增长和持续性内存增长。
13. Windows 完成全部功能验证，Ubuntu 完成构建、测试和核心流程验证。
14. 自动化测试报告、性能记录、演示视频、架构说明和 README 齐全。

## 19. 两周开发里程碑

| 工作日 | 目标 | 当日可验证产物 |
|---:|---|---|
| 1 | 工程骨架、数据类型、协议头和测试环境 | CMake 构建通过，首个协议测试通过 |
| 2 | 协议编解码和单设备模拟器 | 半包、粘包、CRC 测试及单连接遥测 |
| 3 | 设备配置、握手、连接状态机 | 10 台设备独立注册和状态展示 |
| 4 | 心跳、超时、重连和 100 连接测试 | 100 条连接及断线恢复记录 |
| 5 | 最新缓存、设备总览和 5 Hz 刷新 | 500 帧/秒时设备列表可操作 |
| 6 | 实时曲线、控制指令和 RequestTracker | 启停、调速、急停和超时演示 |
| 7 | 规则引擎和告警生命周期 | 6 条设备指标规则与 2 条通信/遥测规则测试 |
| 8 | SQLite 批量存储和历史查询 | 遥测、告警、控制记录可查询 |
| 9 | CSV、日志、性能统计和 Ubuntu 验证 | 导出文件、日志滚动和 Linux 测试记录 |
| 10 | 四小时稳定性测试、文档和演示材料 | 测试报告、README 和演示视频 |

工作日 10 的四小时测试如果暴露稳定性问题，优先修复稳定性并延后视频录制，不增加新功能。

## 20. 最终交付物

- Qt 客户端源码。
- Linux/Windows 可构建的控制台设备模拟器源码。
- CMake 构建配置。
- GoogleTest 测试及测试报告。
- SQLite 建表和升级脚本。
- 自定义协议说明。
- 架构与线程模型说明。
- 四小时稳定性测试报告。
- Windows 演示视频。
- 包含构建、运行、测试、故障注入和演示步骤的 README。

## 21. 发布判定

只有第 18 节全部验收标准通过，且不存在导致崩溃、死锁、静默数据丢失、错误设备控制或无法自动重连的问题时，MVP V1.0 才可标记完成。视觉细节和非关键交互问题可以记录为后续优化，但不得以新增功能替代稳定性修复。
